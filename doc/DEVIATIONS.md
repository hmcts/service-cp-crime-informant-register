# Parity Deviations Register

Behaviour-parity is the rule (constitution Principle I): the observable behaviour of this
service is defined by the legacy informant register function app plus the golden fixtures.
Every deliberate divergence from legacy behaviour MUST be recorded here, with review sign-off,
before the code that introduces it merges. The golden-file harness fails on any divergence
not listed in this register.

| # | Deviation | Legacy behaviour | New behaviour | Rationale | Approved |
|---|---|---|---|---|---|
| 1 | Verified TLS on the payload fetch | Redis client connects with `rejectUnauthorized: false` (certificate checks disabled) | TLS certificate verification enabled, platform CA bundle | Security defect (D14 in the design defect register); not observable in register output | Design sign-off 19 Aug 2026 (Option 2 page: the only improvements in this delivery are transport-level); asserted by `LivePayloadConfigTest.TransportSecurity` |
| 2 | Transport reliability | Final POST errors swallowed at three levels; no retry; hearing silently lost | Failures retry (broker redelivery), then dead-letter with alerting; POST client retries connect/IO/5xx/429; a refusal is parked at once rather than retried to exhaustion; success is `202 Accepted` and any other 2xx is treated as a failure rather than as an accepted command | The single agreed improvement of the lift-and-shift (Option 2 page, "Scope"); changes failure behaviour only, never successful-path output. The 202 rule is the same principle applied to an ambiguous success: a command that was not accepted was not enqueued, and calling it success would lose the register with the log saying it was sent | Design sign-off 19 Aug 2026 |
| 3 | **Waiver — alert wiring deferred (CRA-220 only)** | No signal of any kind: no logs, no metrics, no alerts | CRA-220 ships the full failure signal — a sanitised ERROR log and a failure metric on every failure path, plus countable dead-lettered messages (spec FR-017) — but **no dashboards or alert rules**. Alert wiring lands with the later operability story (the Informant Register Service page on Confluence, "Observability": DLQ alert wiring and reconciliation tooling) | Named waiver of constitution Principle VI's "alerting is required in addition" clause, scoped to this increment. The walking skeleton has no real submission adapter, so there is nothing yet whose failure an on-call alert would act on; the emitted metrics are the prerequisite the alert rules are built from, and shipping them first keeps the waiver time-boxed. Still a strict improvement on legacy silence. Dead-letter **depth** is observed in deployed environments from Azure Monitor's native Service Bus metric `DeadletteredMessages` rather than polled by service code, so the deferred DLQ alert will be wired onto that platform metric; the service's own `informantregister_deadlettered_total{reason=…}` counter ships now alongside it | Gate-2 spec review, 20 Aug 2026 — expires when the operability story lands; follow-up ticket to be raised on the CRA board |
| 4 | Legacy undated cache key read as a second lookup | `getCacheKey` builds one key from the hearing date it was given, and `getResultFromCache` performs exactly one `GET` with it | The dated key is read first; when it holds nothing the legacy undated twin is read before the query side is asked | The producer publishes the payload under both key forms (design doc §2.1; `doc/API_CONTRACTS.md`, `hearingDay` semantics; Option 2 page row 1, "both key forms"), so reading only the dated form sends every legacy-cached hearing to the query API. The cost is that an expired dated key can be answered by a still-live undated twin holding a later share of the same hearing, where legacy would have queried Results — accepted because both keys are written by the same producer in the same operation | Option 2 design page, 19 Aug 2026 (row 1); asserted by `CachedHearingPayloadAdapterTest.RegisteredDeviations` |
| 5 | A cache that cannot be reached is a miss | `getResultFromCache` catches a failed `GET` and returns `null`, but the client is obtained *outside* that catch — so a failed connection (TLS, auth, refused connect) throws out of the activity, the query side is never asked, and the orchestration reports success having produced nothing | Every failure of the cache technology, connection failures included, is reported as a miss, so the query side still gets its turn; only a fault that is not the cache's own escapes | Design rules classify "both Redis and the fallback unavailable" as the transient case, which presumes the fallback is attempted when the cache is unavailable; and legacy's own behaviour on this path is the silent success this service exists to end (deviation 2). Absorbed at the cache adapter and scoped to `RedisException`, so nothing else is swallowed | Design rules ("Processing State Machine", transient list); asserted by `LettuceHearingPayloadCacheIT.Unreachable` |
| 6 | No fragments is an empty list, not `undefined` | `SetInformantRegisterBuilder.build()` returns `undefined` twice over — for a hearing with neither `prosecutionCases` nor `courtApplications`, and for one whose loop yields no fragments. `InformantRegisterOrchestrator`'s `if (informantRegisters)` then skips subscription matching, the outbound mapping and the POST, and the orchestration still reports success | `RegisterBuilder.build()` returns an empty list. The pipeline records the run COMPLETED with the reason `no-authorities` | Nothing is submitted on either side and no register content differs: the divergence is that the port's answer is a value the state machine can record rather than an absence every caller has to test for. `design_rules.md` ("Processing State Machine") requires exactly that — a hearing that legitimately produces no authorities ends COMPLETED with `no-authorities`, a business outcome and not an error. Pinned by `RegisterBuilderParityTest.assertLegacyProducedNothing`, which asserts the empty list on cases 08 and 09 and fails on a populated one — a port that found authorities the legacy did not is an unregistered divergence and must not reach a prosecuting authority. Note the boundary: this covers a hearing the legacy has *no register for*. A hearing the legacy *throws* on is a different case and is covered by entry 7 — those now surface as a non-transient failure and dead-letter, never as an empty result | **Sign-off pending** — raised by the Story-2 code review, 22 Aug 2026. The behaviour predates this entry; the entry records it. To be tabled at the next parity review |
| 7 | A hearing the transformation cannot read is reported, not swallowed | `setInformantRegisterHandler` wraps the whole build in `try { … } catch (err) { context.error(…) }` and returns `undefined` (`SetInformantRegister/index.js:159`). Every `TypeError` inside the port — an unguarded `defendant.offences.forEach`, a `courtCentre` that is `null`, a `DateService.parse` that cannot read an ordered date — is therefore absorbed. `InformantRegisterOrchestrator`'s `if (informantRegisters)` skips the rest of the flow and the orchestration still returns `Success: true`. The hearing produces no register for any authority, and nothing anywhere records that it was meant to | The same conditions raise `TransformationFailedException`, classified `NON_TRANSIENT`. The pipeline records the request `FAILED` with reason `TRANSFORMATION_FAILED` and dead-letters the delivery with a bounded reason. The delivery budget is not spent first: no redelivery re-reads the payload differently | **The scope is failure handling only, and the boundary matters.** Where the legacy throws, this refuses; where the legacy produces no register *without* throwing, this returns an empty list and completes (entry 6). No hearing that the legacy turns into a register is turned into anything different here — the refusals are, by construction, exactly the inputs on which the legacy emits nothing. This is the "end of swallowed errors" half of the transport-reliability change the Option 2 page sanctions, which entry 2 records for the *final POST* only; the transformation's own swallow is a second site of the same defect and is recorded separately rather than read into entry 2's wording. Constitution Principle VI: a hearing that fails must be visible and replayable, and dead-lettering is what makes the parked request recoverable through the documented `FAILED` → replay path. Pinned by the refusal assertions in `JsonTest`, `DefendantContextBuilderTest`, `VocabularyBuilderTest`, `HearingDatesTest`, `SubscriptionMatcherParityTest` (for the two unguarded dereferences in `InformantRegisterSubscriptions/index.js` — the register-date lookup at `:18` and `registerDefendants[0]` at `:46`, blind spot BS-12) and `SubscriptionRulesParityTest.NullElementsAreRefused`, each of which cites the legacy line it reproduces. **An array *element* the legacy reads a property off is covered by this entry too**, and was the one place the port had been the more forgiving one: a `null` among `nowSubscriptions` (`index.js:28`), among the candidate or child subscriptions, among the judicial results (`SubscriptionsService.js:213`, `:234`, `:286`) or among a result's prompts (`:224`, `:287`) is a `TypeError` in the legacy and produces no register for any authority, so skipping it and matching the rest would emit recipients the legacy never emitted. `Json.dereferencedElement` reproduces the legacy's *reach* rather than refusing wholesale — `some`/`find` stop at the first answer, `filter` completes its pass, and an empty reference-data list never runs its callback at all — so nothing the legacy still answers is refused here | **Sign-off pending** — raised by the Story-2 code review, 22 Aug 2026, which found the behaviour attributed to entry 2. To be tabled at the next parity review alongside entry 6 |
| 8 | **`Retry-After` is honoured in delta-seconds only, and bounded** (sub-decision of #2) | No retry of any kind, so no `Retry-After` was read: a 429 was swallowed with everything else | A 429's `Retry-After` is acted on when it is delta-seconds, bounded above by `informantregister.results.max-backoff`. An HTTP-date, an unreadable value, or no header at all falls back to the exponential back-off, under the same ceiling | Recorded because #2 said "the delay the server asked for, capped" without saying which forms are read. Acting on an HTTP-date would subtract a remote clock's idea of now from this pod's, and a server a few minutes ahead would park a run past the claim lease it holds (`design_rules.md`, "Claim timing") — the header is therefore classified before it is read rather than parsed and caught. The ceiling exists for the same reason and applies to a server-supplied delay exactly as it does to the back-off. Asserted by `ResultsCommandGatewayTest`: an HTTP-date, an unparseable value and a value beyond the ceiling each have their own case | Raised at the story-3 code review, 22 Aug 2026; sub-decision of #2 (design sign-off 19 Aug 2026), to be confirmed with the design authority |
| 9 | **An unmapped verdict code yields a verdict with no type, not a null one** | `VerdictCodeMapping.getVerdictType` returns `null` for every code outside `G`, `N`, `PSJ` (and for the literal `None`), and `OffenceMapper.js:69-73` writes that straight into the verdict object. `JSON.stringify` keeps an explicit null, so the POSTed body carries `"verdictType": null` inside a component the frozen contract types as a `string` — a schema violation the consumer would have to reject, on a hearing that is otherwise fine | The verdict is still emitted, still carrying the `verdictCode` the payload sent and the `verdictDate` beside it; only the derived `verdictType` is absent rather than null, under `@JsonInclude(NON_NULL)` | The typed outbound tree cannot represent a null in a string component and must not try: widening it would mean relaxing somebody else's closed contract locally, which constitution Principle III forbids. Nothing about *which* verdicts reach a register changes, and nothing about the two components the payload actually supplies changes — the difference is confined to a derived value the legacy could not name either. Absent is also the shape the schema calls valid for a component it does not require. Asserted by `OffenceMapperTest.PinnedOddities.an_unmapped_verdict_code_yields_a_verdict_with_no_type`, which pins both halves: the code survives, the type is absent | **Sign-off pending** — raised by the aggregation-mapper port, 23 Aug 2026. To be tabled at the next parity review alongside entries 6 and 7 |
| 10 | **A register component that cannot be typed is parked, not POSTed** | The aggregation carries the fragment's identifiers and timestamps as whatever strings the earlier steps produced, and `axios.post` sends them. A hearing id that is not a UUID, or a register date that came out as the literal `"Invalid dateZ"` (D9 + D10, parity-pack `s05`), is therefore POSTed into a `uuid`- or `date-time`-typed component and rejected by the consumer's schema — which the legacy neither inspects nor logs, so the register is lost silently | The same values raise `TransformationFailedException`, classified `NON_TRANSIENT`. The request is recorded `FAILED` and the delivery dead-lettered with a bounded reason | The consumer's own binding types these as `UUID` and `ZonedDateTime`, so the typed tree has nowhere to put a value of another shape and inventing one would be worse than refusing. **The outcome is the same either way — no register reaches the authority** — so this changes only whether the failure is visible: it is the "end of swallowed errors" principle of entry 2 applied at the last boundary, exactly as entry 7 applies it at the first. The values themselves are never quoted into the reason, only the component's name (`research.md` §11, bounded reason codes). Note the narrowness, and its one exception. A *well-formed* value keeps its rendering, D9's misleading `Z` included — a parsed timestamp re-renders as the string it was parsed from, which is what the golden-parity twins confirm. The exception is letter case: an upper-case identifier is well-formed, the consumer's schema accepts it and the legacy forwards it unchanged, but a `UUID` has only one rendering and it comes back lower-cased. That is carried rather than refused, because refusing a value the legacy sends successfully would lose a register; it is recorded here because the "byte for byte" claim was not true of it. Note also what the refusal is *not* built on: `UUID.fromString` accepts shorthand — `1-1-1-1-1` parses and renders as `00000001-0001-0001-0001-000000000001` — so the shape is checked against the canonical 8-4-4-4-12 form first, and only then parsed. Inventing an identifier out of a value the legacy would have had rejected downstream would file the register against a hearing nobody has, which is worse than parking it. Asserted by `AggregationMapperTest.TypedBoundary`, whose cases cover a non-identifier, a shorthand identifier, an upper-case one and an unreadable timestamp | **Sign-off pending** — raised by the aggregation-mapper port, 23 Aug 2026. Depends on the open Results-team question the parity pack records for `s05`: whether `"Invalid dateZ"` should be reproduced or refused. Refusing is recorded here so the choice is visible; the answer may retire this entry |
| 11 | **A value the typed outbound tree cannot carry is absent, never approximated** | Three shapes of the same legacy habit — assigning whatever the payload held into an untyped object and letting `JSON.stringify` sort it out. (a) An explicit JSON `null` is *assigned*, not skipped, so `RecipientMapper.js:15-22` emits `"emailAddress1":null` and every other mapper can emit a null into any leaf. (b) `OffenceMapper.js:63` copies `offence.orderIndex` across whatever it is — `1.5`, a string, a value past 2³². (c) `ResultDataMapper.js:21,23` call `.toString()` on a float64, so `1e21` prints as `1e+21`, `1e-7` as `1e-7`, and `9007199254740993` has already become `9007199254740992` before it is printed | (a) `@JsonInclude(NON_NULL)` across the whole document tree turns an explicit null into an omitted key. (b) `orderIndex` is carried only when it is an integral number that fits an `int`; anything else is absent. (c) duration values are rendered from the exact decimal the payload sent (`USE_BIG_DECIMAL_FOR_FLOATS`, trailing zeros stripped, plain notation) | The generalisation of entry 9, which recorded only the `verdictType` instance of (a). Every one of these components is typed by the consumer's binding and declared non-null by a closed schema, so the typed tree has no way to say "null" and must not invent one; the outcome is identical, because a body carrying a null in a string component is a body the consumer rejects. (b) is the case that matters, and it is a correctness fix rather than a shape choice: truncating `1.5` to `1` would produce a body that *passes* validation carrying an index the payload never sent, which is the one answer neither the legacy nor the contract gives. (c) is a difference only at JavaScript's exponential thresholds and beyond float64's exact-integer range — reproducing ECMAScript's number-to-string algorithm means rounding every duration through a `double` first, a larger behaviour change than the one it fixes, so the gap is recorded rather than approximated. Asserted by `AggregationMapperTest.RecipientTwins.a_null_address_is_omitted_from_the_wire_rather_than_written_as_null` and `OffenceMapperTest.OrderIndex` | **Sign-off pending** — raised by the aggregation-mapper review, 23 Aug 2026. To be tabled at the next parity review alongside entries 6, 7, 9 and 10 |
| 12 | **An identifier list cannot tell an absent id from an explicit null** | `defendantBase.cases.push(prosecutionCase.id)` (`DefendantContextBaseService.js:67`) pushes `undefined` for a case with no `id` and `null` for one whose `id` is JSON null, and `ProsecutionCaseOrApplicationMapper.js:53-55` then selects with `pcase.id === caseId`, which keeps those two apart | `RegisterDefendant.cases` and `.applications` are `List<String>`, where both forms are the same absent element, so an id list holding one form can select a hearing member carrying the other — producing an entry the legacy omits, or attributing one to the wrong case | Reaching it takes a payload that uses *both* degenerate forms, on two different cases in the same hearing; every single-form payload behaves identically, because `undefined === undefined` and `null === null` are both true and both collapse to the same Java comparison. Closing it means changing what the fragment records, not how the mapper compares — the id lists would have to distinguish the two, which changes the shape of every golden fragment and is a decision for the parity review, not a change made in passing while fixing something else. Recorded so the collapse is visible rather than discovered; the boundary is documented on `CaseOrApplicationMapper.find` | **Sign-off pending, and a model decision is required** — raised by the aggregation-mapper review, 23 Aug 2026 |
| 13 | **Date forms `moment` resolves through `new Date()` that this port does not read** | `moment.tz(value, 'Europe/London')` falls back to V8's `Date` parser for anything it does not recognise as ISO or RFC 2822, so `2020-06` reads as 1 June and `2020/06/19 10:30` as that morning | `HearingDates` reproduces the ISO forms and the bare leading-date fallback, and renders anything else as the literal `"Invalid date"` — which is what `moment` renders for a value it genuinely cannot read, but not for these | The correct half of this change is the one that made the gap visible: `localDate` and `localDateTime` now *render* `"Invalid date"` where they previously threw, because `moment` does not throw — an unreadable sitting day (`CourtSessionMapper.js:26-28`) or next-hearing start (`ResultDataMapper.js:15`) is formatted into the register and the hearing carries on, and refusing there would park a hearing the legacy renders. What remains is the residue: V8's date parser is lenient in ways no port can reproduce by guessing at it, so the forms above render as invalid rather than as their date. Both call sites read the payload directly, so the residue is reachable; it is recorded rather than approximated, and it is bounded — the affected values are ones the frozen contract types as `date-time` and would reject in either rendering. `DateService.parse` is untouched and still throws, because it really does (`HearingDatesTest.Ordering`). Asserted by `HearingDatesTest.LocalDateTime` and `.Localised` | **Sign-off pending** — raised by the aggregation-mapper review, 23 Aug 2026. The narrowing question belongs with the same Results-team `s05` conversation entry 10 depends on |
| 14 | **A reference-data outage is reported, not shipped as a register that reaches nobody** | `ReferenceDataService.getSubscriptionsMetadata` wraps the now-subscriptions GET in a try that returns `null` on any failure (`:52`), and `InformantRegisterSubscriptions/index.js:22` cannot tell that `null` from a reference-data answer carrying no subscriptions. So a 5xx, a 429, a 404 or a refused connection produces the fragments untouched, the aggregation files a document with no `recipients` component at all, and `ProcessOutboundInformantRegister` POSTs it. The register is accepted, recorded, and read by nobody; nothing anywhere says the recipients were missing because reference data was down. Twenty corpus cases record it, and the parity pack pins it as `d03` | The subscriptions port raises `ReferenceDataUnavailableException`, classified `TRANSIENT` with the reason `REFERENCE_DATA_UNAVAILABLE`. The pipeline records the request RETRYING and hands the delivery back; on the final permitted delivery it is recorded FAILED and parked. Nothing is POSTed | This is the same principle as entry 2 and entry 7, applied to the third and last site of the same defect: the legacy catches an error, carries on, and the loss is invisible. It is the *only* place in the chain where the port's documents deliberately differ from the recording, which is why it is registered separately rather than read into entry 2 — and the difference is in the safe direction, because no register reaches an authority either way. Transient rather than non-transient because reference data being unreachable now says nothing about the next delivery, and the register is perfectly buildable once it answers. The pinning entry `d03` states the obligation directly: a port that produces those documents and reports success "has reproduced the defect instead of the behaviour", and the test must fail it. Asserted by `PinnedOddityTest.d03_refdata_failure_is_classified_rather_than_degraded`, by the twenty `refdata-unavailable` cases in `RegisterTransformerParityTest`, and by `DistributionPipelineTest.ProducedAuthorities` for the settlement half. **Note what is not decided here**: the retry *policy* — how many attempts, and whether a 429's `Retry-After` is honoured — belongs to the adapter and to entry 8, and no golden document can tell 429-attempted-once from 500-attempted-twice | **Sign-off pending** — raised by the transformation-chain integration, 23 Aug 2026. To be tabled at the next parity review alongside entries 6, 7, 9, 10, 11, 12 and 13 |
| 15 | **`hearingStartTime` is rendered as the time of day it was, with the offset that says which hour that was** | `CourtSessionMapper.getHearingStartTime()` (`CourtSessionMapper.js:25-30`) returns `DateService.getLocalDateTime(hearingDays[0].sittingDay)`, which is `moment_timeZone.tz(value, "Europe/London").format('YYYY-MM-DDTHH:mm:ss') + 'Z'` (`NowsHelper/service/DateService.js:36-38`). Two things are wrong with the result and both reach the wire. It is a full **date-time** in a component `informantRegisterHearing.json` declares to be `{"type": "string", "format": "time"}` — an RFC 3339 `full-time`, a time of day and an offset — so no value this component has ever carried has satisfied its own schema. And the `Z` is a literal, appended whatever the offset was: a sitting at 14:30 in June renders `2020-06-19T14:30:00Z`, labelling British Summer Time as UTC and naming an hour that did not happen at that instant. That is defect D9. The oracle carries it on 523 golden values — 515 in the recorded corpus, 5 in the pinning pack, 3 in the Jest fixtures | The same wall clock, rendered as the RFC 3339 `full-time` the schema asks for, carrying London's **true** offset on that date — read from the `Europe/London` zone rules, never assumed. A 14:30 sitting in June is `14:30:00+01:00`; the same wall clock in January is `14:30:00Z`. `HearingDates.localFullTime` produces it and `CourtSessionMapper` is its only call site | **Both halves of the decision are load-bearing.** *Visually correct*: the digits are the ones the legacy renders and the ones a clerk read off the courtroom clock — 14:30 stays 14:30, so nothing a human reads changes. *Semantically correct*: the offset now says which hour of the day those digits were, which the literal `Z` actively lied about for seven months of every year. *Schema-compliant*: this is the first rendering of this component that satisfies the declared format, and `InformantRegisterDocumentTest.OpenQuestions` flips from pinning the refusal to pinning the compliance. **The scope is this component alone.** `registerDate` and `hearingDate` keep D9's rendering and the parity pack's `d09` pin — which asserts on those two and on `fileName`, not on this — is untouched. So is the parse: an unreadable sitting day still renders the literal `"Invalid dateZ"`, because the decision was about how a readable time is labelled and the unreadable case is entry 13's territory, not something to change while passing. **The zero offset is written `Z`, not `+00:00`**, because that is what java.time's own `XXX` offset pattern emits (the rendering `OffsetTime#toString()` and `ISO_OFFSET_TIME` use); the seconds are forced because `ISO_OFFSET_TIME` drops a zero seconds field and RFC 3339 requires it. **How the parity harness holds it.** The recorded `expected.json` files are the Node oracle's truth and are neither regenerated nor edited, so the golden values still carry D9's rendering. The comparator therefore checks this one component by *derivation* rather than by equality (`RegisteredFieldDeviations`, wired into `JsonParity`): it reads the wall clock back out of the oracle's own string, asks the `Europe/London` rules for the offset on that date, and requires the port to have written exactly that. It is not an exclusion — a hard-coded `+01:00` fails 108 differential cases, and `JsonParityTest.RegisteredFieldDeviation` pins the rejections case by case, the wrong-season offset included. Asserted by `HearingDatesTest.LocalFullTime` (both seasons, both sides of the spring gap, both occurrences of the repeated autumn hour, the forced seconds, the unchanged `"Invalid dateZ"`), `HearingVenueMapperTest` at the mapper's own call site, and the 384-case differential suite. **⚠ Results must be told.** The CSV column the prosecuting authorities read changes shape, from a full date-time to a time of day. Nothing else in the document moves, and the schema goes from violated to satisfied, but an authority parsing the old shape positionally will see a shorter value — this needs to reach the Results team and the authorities' own change window before it ships beyond the POC | **Project-owner decision, 2026-08-23** — "For time lets use visually correct and semantically correct value - 14:30:00+01:00". Decided, not pending. Outstanding action: notify `cpp-context-results` of the column-shape change |
| 16 | **A message that names no user runs under the system identity, where the legacy passed the envelope's `userId` through whatever it was** | Attribution is the envelope's. `InformantRegisterEventGridTrigger/index.js:15` and `InformantRegisterQueueTrigger/index.js:17` copy `eventGridEvent.data.userId` into the orchestration as `cjscppuid` — unconditionally, so an envelope that carried none produced an `undefined` `cjscppuid` rather than a substituted identity; how often that happened is not established by the source and is not asserted anywhere here. And `InformantRegisterOrchestrator/index.js:13,31,46` threads that one value into the payload read, the now-subscriptions read (`ReferenceDataService.js:44`) and the `add-informant-register` POST (`ProcessOutboundInformantRegister/index.js:21`). A **retry** re-delivers the same envelope, so Event Grid's own redelivery of a failed event arrives carrying the same `userId` and the retried run is attributed to the original sharing user exactly as the first attempt was | Everything above is restored and needs no entry: the message carries an optional `userId`, and a run that has one makes all three calls as that user. **The replay is not an exception to that.** The documented replay procedure *requires* the dead-lettered body to be re-sent *verbatim*, changing only the broker `messageId` (`doc/API_CONTRACTS.md`, "Replay rule"), so a replay that follows it still names the user the original named and is attributed to them — automatically, because attribution is read from the message and from nothing else. That is a procedure this service relies on, not a property it can enforce: nothing here checks that the operator or tool copied the original bytes, and a replay that does not is simply a message naming no user, which is the residue below. The residue is a message that carries **no user at all**, which happens three ways: a message published during the transition window, before the producer sent the field; a replay hand-built without the original body; and the deliberate operator escape hatch — where the original user has been deactivated and their identity would be refused downstream, support re-sends the body with the `userId` field removed. Those runs are made under the service's configured system identity | The residue is confined to a message nobody attributed, and for one there is nothing else to be made as. A broker redelivery of the *same* message was never affected — the body, `userId` included, is redelivered unchanged — and a verbatim replay is not affected either, which is the point of re-sending the body rather than rebuilding it. What remains is the transition window, which closes when the producer ships, and a message built by hand: the replay tool cannot assert an identity it was not given, and carrying one across a replay *without* the body would mean persisting a user identifier in the processed log — storing PII this service has no other need for — which was considered and rejected. The escape hatch is the same mechanism used deliberately: a refused identity would fail every call of the run, and the system identity is what makes the register recoverable at all. Nothing about *which* register reaches *which* authority changes in any of these cases, and the register content is identical either way; what differs is the `CJSCPPUID` on the calls. Pinned by `FailedReplayIT.ReplayAttribution` — a request driven to `FAILED`, then replayed under a fresh identity: *given* a verbatim body it runs as the user that body names, and given the body with `userId` stripped it runs as the system and is still the same unit of work. What the test pins is the consumer's response to each body, which is all this side owns; that a replay is verbatim in the first place is the procedure's job — by `DistributionPipelineTest.a_run_the_message_named_no_user_for_should_be_made_as_the_system`, and on the wire by the fallback cases in `ResultsCommandGatewayTest`, `ReferenceDataNowSubscriptionsClientTest` and `ResultsQueryHearingPayloadClientTest` | **Project-owner decision, 2026-08-23** — legacy attributed downstream calls to the user who shared the results and the new system keeps that behaviour; `userId` is restored as an optional field with the system identity as fallback, and the replay procedure requires the body to be re-sent verbatim so that an attributed request stays attributed on recovery (option 1). Persisting the user in the processed log to attribute a replay (option 2) was considered and rejected: it stores PII the service has no other need for. Decided, not pending |

Entries below this line are added per change, newest first. Renumber never; append only.
